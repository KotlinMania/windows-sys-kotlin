// port-lint: source Windows/Wdk/mod.rs
package io.github.kotlinmania.windowssys.windows.wdk

/**
 * Windows Driver Kit (WDK) bindings.
 *
 * Upstream submodules are feature-gated; this Kotlin port models the same
 * module tree as packages.
 */
public object Wdk
